{% extends 'admin/layout.html.twig' %}

{% block title %}{{ user ? 'Modifier' : 'Ajouter' }} un utilisateur - Najahni{% endblock %}

{% block main %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold mb-0"><i class="bi bi-person-{{ user ? 'gear' : 'plus' }} me-2"></i>{{ user ? 'Modifier' : 'Ajouter' }} un utilisateur</h2>
    <a href="{{ path('admin_users') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Retour</a>
</div>

<div class="card border-0 shadow-sm" style="border-radius:1rem">
    <div class="card-body p-4">
        <form method="post">
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Prénom</label>
                    <input type="text" name="firstname" class="form-control" value="{{ user ? user.firstname }}" required>
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Nom</label>
                    <input type="text" name="lastname" class="form-control" value="{{ user ? user.lastname }}" required>
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Email</label>
                    <input type="email" name="email" class="form-control" value="{{ user ? user.email }}" required>
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Téléphone</label>
                    <input type="text" name="phone" class="form-control" value="{{ user ? user.phone }}">
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Rôle</label>
                    <select name="role" class="form-select" required>
                        {% for r in ['ADMIN', 'ENTREPRENEUR', 'MENTOR', 'INVESTISSEUR'] %}
                            <option value="{{ r }}" {{ user and user.role == r ? 'selected' }}>{{ r }}</option>
                        {% endfor %}
                    </select>
                </div>
                {% if not user %}
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Mot de passe</label>
                    <input type="password" name="password" class="form-control" value="najahni123">
                    <small class="text-muted">Par défaut : najahni123</small>
                </div>
                {% endif %}
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Entreprise</label>
                    <input type="text" name="company_name" class="form-control" value="{{ user ? user.companyName }}">
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Adresse</label>
                    <input type="text" name="address" class="form-control" value="{{ user ? user.address }}">
                </div>
                <div class="col-12">
                    <label class="form-label fw-semibold">Bio</label>
                    <textarea name="bio" class="form-control" rows="3">{{ user ? user.bio }}</textarea>
                </div>
            </div>
            <button type="submit" class="btn btn-nj mt-4"><i class="bi bi-check-lg me-2"></i>{{ user ? 'Enregistrer' : 'Créer' }}</button>
        </form>
    </div>
</div>
{% endblock %}
